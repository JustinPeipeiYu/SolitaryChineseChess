using System.Runtime.CompilerServices;

namespace SolitaryChineseChess.Pages
{
    public class Guard : Piece
    {
        public Guard(double x, double y, double w, int c, int n)
        {
            X = x;
            Y = y;
            W = w;
            C = c;
            N = n;
        }
    }
}

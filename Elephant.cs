using System.Runtime.CompilerServices;

namespace ChessAlly.Pages
{
    public class Elephant : Piece
    {
        public Elephant(double x, double y, double w, int c, int n)
        {
            X = x;
            Y = y;
            W = w;
            C = c;
            N = n;
        }
    }
}

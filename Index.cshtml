@page 
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<!---->
<head>
    <style type="text/css">
        canvas{border:thin solid black}
        p{background: gray;
          color:white;
          padding:10px;
          margin:5px;
          border:thin solid black;
        }
    </style>
</head>
<body>
    <canvas id="canvas" width="1000" height="600">
    </canvas>

    
    <script>
        
        var ctx = document.getElementById("canvas").getContext("2d");
        
        function drawHorizontalLine(ctx,x1, x2, y) {
            ctx.beginPath();
            ctx.moveTo(x1, y);
            ctx.lineTo(x2, y);
            ctx.stroke();
            ctx.closePath();
        }
        function drawVerticalLine(ctx,x, y1, y2) {
            ctx.beginPath();
            ctx.moveTo(x, y1);
            ctx.lineTo(x, y2);
            ctx.stroke();
            ctx.closePath();
        }
        function drawDiagonalLine(ctx,x1, x2, y1, y2) {
            ctx.beginPath();
            ctx.moveTo(x1, y1);
            ctx.lineTo(x2, y2);
            ctx.stroke();
            ctx.closePath();
        }
        function drawGameBoard(ctx,x, y) {
            ctx.strokeStyle = "black";
            ctx.lineWidth = "4";
            for (let i = 0; i < 10; i++) {
                drawHorizontalLine(ctx,x,x + 8 * 50, y + i * 45);
            }
            for (let i = 1; i < 8; i++) {
                drawVerticalLine(ctx,x + i * 50, y, y + 4 * 45);
                drawVerticalLine(ctx,x + i * 50, y + 5 * 45, y + 9 * 45);          
            }
            drawVerticalLine(ctx,x, y, y + 9 * 45);
            drawVerticalLine(ctx,x + 8 * 50, y, y + 9 * 45);
            drawDiagonalLine(ctx,x + 3 * 50, x + 5 * 50, y, y + 2 * 45);
            drawDiagonalLine(ctx,x + 3 * 50, x + 5 * 50, y + 2 * 45, y);
            drawDiagonalLine(ctx,x + 3 * 50, x + 5 * 50, y + 7 * 45, y + 9 * 45);
            drawDiagonalLine(ctx,x + 3 * 50, x + 5 * 50, y + 9 * 45, y + 7 * 45);
        }
               
        function drawBlackGeneral(ctx,x, y) {
            drawPiece(ctx,x,y);
            ctx.fillStyle = "black";
            ctx.lineWidth = "1";
            ctx.font = "20px sans-serif";
            ctx.fillText("将", x - 10, y);
            ctx.strokeText("将", x - 10, y);
            ctx.font = "10px sans-serif";
            ctx.fillText("jiàng", x - 10, y + 10);
            ctx.strokeText("jiàng", x - 10, y + 10);
        } 
        function drawRedGeneral(ctx,x, y) {
            drawPiece(ctx,x,y);
            ctx.strokeStyle = "red";
            ctx.fillStyle = "red";
            ctx.lineWidth = "1";
            ctx.font = "20px sans-serif";
            ctx.fillText("帅", x - 10, y);
            ctx.strokeText("帅", x - 10, y);
            ctx.font = "10px sans-serif";
            ctx.fillText("shuài", x - 10, y + 10);
            ctx.strokeText("shuài", x - 10, y + 10);
        } 
        function drawBlackGuard(ctx,x,y) {
            drawPiece(ctx,x,y);
            ctx.fillStyle = "black";
            ctx.lineWidth = "1";
            ctx.font = "20px sans-serif";
            ctx.fillText("士", x - 10, y+2);
            ctx.strokeText("士", x - 10, y+2);
            ctx.font = "10px sans-serif";
            ctx.fillText("shì", x - 5, y + 12);
            ctx.strokeText("shì", x - 5, y + 12);
        }
        function drawRedGuard(ctx,x, y) {
            drawPiece(ctx,x,y);
            ctx.strokeStyle = "red";
            ctx.fillStyle = "red";
            ctx.lineWidth = "1";
            ctx.font = "20px sans-serif";
            ctx.fillText("仕", x - 10, y+2);
            ctx.strokeText("仕", x - 10, y+2);
            ctx.font = "10px sans-serif";
            ctx.fillText("shì", x - 5, y + 12);
            ctx.strokeText("shì", x - 5, y + 12);
        }
        function drawBlackElephant(ctx,x, y) {
            drawPiece(ctx,x,y);
            ctx.fillStyle = "black";
            ctx.lineWidth = "1";
            ctx.font = "20px sans-serif";
            ctx.fillText("象", x - 10, y);
            ctx.strokeText("象", x - 10, y);
            ctx.font = "10px sans-serif";
            ctx.fillText("xiàng", x - 10, y + 10);
            ctx.strokeText("xiàng", x - 10, y + 10);
        }
        function drawRedElephant(ctx,x,y) {
            drawPiece(ctx,x,y);
            ctx.strokeStyle = "red";
            ctx.fillStyle = "red";
            ctx.lineWidth = "1";
            ctx.font = "20px sans-serif";
            ctx.fillText("相", x - 10, y);
            ctx.strokeText("相", x - 10, y);
            ctx.font = "10px sans-serif";
            ctx.fillText("xiàng", x - 10, y + 10);
            ctx.strokeText("xiàng", x - 10, y + 10);
        }
        function drawBlackHorse(ctx,x, y) {
            drawPiece(ctx,x,y);
            ctx.fillStyle = "black";
            ctx.lineWidth = "1";
            ctx.font = "20px sans-serif";
            ctx.fillText("马", x - 10, y+1);
            ctx.strokeText("马", x - 10, y+1);
            ctx.font = "10px sans-serif";
            ctx.fillText("mǎ", x - 6, y + 12);
            ctx.strokeText("mǎ", x - 6, y + 12);
            
        }
        function drawRedHorse(ctx,x, y) {
            drawPiece(ctx,x,y);
            ctx.strokeStyle = "red";
            ctx.fillStyle = "red";
            ctx.lineWidth = "1";
            ctx.font = "20px sans-serif";
            ctx.fillText("马", x - 10, y + 1);
            ctx.strokeText("马", x - 10, y + 1);
            ctx.font = "10px sans-serif";
            ctx.fillText("mǎ", x - 6, y + 12);
            ctx.strokeText("mǎ", x - 6, y + 12);
        }
        function drawBlackRook(ctx,x,y) {
            drawPiece(ctx,x, y);
            ctx.fillStyle = "black";
            ctx.lineWidth = "1";
            ctx.font = "20px sans-serif";
            ctx.fillText("车", x - 10, y + 1);
            ctx.strokeText("车", x - 10, y + 1);
            ctx.font = "10px sans-serif";
            ctx.fillText("jū", x - 4, y + 12);
            ctx.strokeText("jū", x - 4, y + 12);
        }
        function drawRedRook(ctx,x,y) {
            drawPiece(ctx,x, y);
            ctx.strokeStyle = "red";
            ctx.fillStyle = "red";
            ctx.lineWidth = "1";
            ctx.font = "20px sans-serif";
            ctx.fillText("车", x - 10, y + 1);
            ctx.strokeText("车", x - 10, y + 1);
            ctx.font = "10px sans-serif";
            ctx.fillText("jū", x - 4, y + 12);
            ctx.strokeText("jū", x - 4, y + 12);
        }
        function drawBlackCannon(ctx,x, y) {
            drawPiece(ctx,x, y);
            ctx.fillStyle = "black";
            ctx.lineWidth = "1";
            ctx.font = "20px sans-serif";
            ctx.fillText("砲", x - 10, y + 1);
            ctx.strokeText("砲", x - 10, y + 1);
            ctx.font = "10px sans-serif";
            ctx.fillText("pào", x - 7, y + 12);
            ctx.strokeText("pào", x - 7, y + 12);
        }
        function drawRedCannon(ctx,x, y) {
            drawPiece(ctx,x, y);
            ctx.strokeStyle = "red";
            ctx.fillStyle = "red";
            ctx.lineWidth = "1";
            ctx.font = "20px sans-serif";
            ctx.fillText("炮", x - 10, y + 1);
            ctx.strokeText("炮", x - 10, y + 1);
            ctx.font = "10px sans-serif";
            ctx.fillText("pào", x - 7, y + 12);
            ctx.strokeText("pào", x - 7, y + 12);
        }
        function drawBlackSoldier(ctx,x, y) {
            drawPiece(ctx,x, y);
            ctx.fillStyle = "black";
            ctx.lineWidth = "1";
            ctx.font = "20px sans-serif";
            ctx.fillText("卒", x - 10, y + 1);
            ctx.strokeText("卒", x - 10, y + 1);
            ctx.font = "10px sans-serif";
            ctx.fillText("zú", x - 4, y + 12);
            ctx.strokeText("zú", x - 4, y + 12);
        }
        function drawRedSoldier(ctx,x, y) {
            drawPiece(ctx,x, y);
            ctx.strokeStyle = "red";
            ctx.fillStyle = "red";
            ctx.lineWidth = "1";
            ctx.font = "20px sans-serif";
            ctx.fillText("兵", x - 10, y + 1);
            ctx.strokeText("兵", x - 10, y + 1);
            ctx.font = "10px sans-serif";
            ctx.fillText("bīng", x - 8, y + 12);
            ctx.strokeText("bīng", x -8, y + 12);
        }
        
        function drawPiece(ctx, x, y){
            ctx.strokeStyle = "black";
            ctx.fillStyle = "#FFE5B4";
            ctx.beginPath();
            ctx.lineWidth = "4";
            ctx.arc(x, y, 20, 0, Math.PI * 2, true);
            ctx.fill();
            ctx.stroke();
            ctx.closePath();
        }
        /*
        function drawPiece(ctx,name, color, x, y) {
            ctx.strokeStyle = "black";
            ctx.fillStyle = "#FFE5B4";
            ctx.beginPath();
            ctx.lineWidth = "4";
            ctx.arc(x, y, 20, 0, Math.PI * 2, true);
            ctx.fill();
            ctx.stroke();
            ctx.closePath();
            ctx.lineWidth = "1";
            ctx.font = "20px sans-serif";
            
            if (color=="black"){
                ctx.fillStyle = "black";
                if (name=="general"){
                    ctx.fillText("将", x - 10, y);
                    ctx.strokeText("将", x - 10, y);
                    ctx.font = "10px sans-serif";
                    ctx.fillText("jiàng", x - 10, y + 10);
                    ctx.strokeText("jiàng", x - 10, y + 10);
                } else if (name=="guard"){
                    ctx.fillText("士", x - 10, y + 2);
                    ctx.strokeText("士", x - 10, y + 2);
                    ctx.font = "10px sans-serif";
                    ctx.fillText("shì", x - 5, y + 12);
                    ctx.strokeText("shì", x - 5, y + 12);
                } else if (name=="elephant"){
                    ctx.fillText("象", x - 10, y);
                    ctx.strokeText("象", x - 10, y);
                    ctx.font = "10px sans-serif";
                    ctx.fillText("xiàng", x - 10, y + 10);
                    ctx.strokeText("xiàng", x - 10, y + 10);
                } else if (name=="horse"){
                    ctx.fillText("马", x - 10, y + 1);
                    ctx.strokeText("马", x - 10, y + 1);
                    ctx.font = "10px sans-serif";
                    ctx.fillText("mǎ", x - 6, y + 12);
                    ctx.strokeText("mǎ", x - 6, y + 12);
                } else if (name=="rook"){
                    ctx.fillText("车", x - 10, y + 1);
                    ctx.strokeText("车", x - 10, y + 1);
                    ctx.font = "10px sans-serif";
                    ctx.fillText("jū", x - 4, y + 12);
                    ctx.strokeText("jū", x - 4, y + 12);
                } else if (name=="cannon"){
                    ctx.fillText("砲", x - 10, y + 1);
                    ctx.strokeText("砲", x - 10, y + 1);
                    ctx.font = "10px sans-serif";
                    ctx.fillText("pào", x - 7, y + 12);
                    ctx.strokeText("pào", x - 7, y + 12);
                } else if (name=="soldier"){
                    ctx.fillText("卒", x - 10, y + 1);
                    ctx.strokeText("卒", x - 10, y + 1);
                    ctx.font = "10px sans-serif";
                    ctx.fillText("zú", x - 4, y + 12);
                    ctx.strokeText("zú", x - 4, y + 12);
                }
            } else if (color=="red"){
                ctx.strokeStyle = "red";
                ctx.fillStyle = "red";
                if (name=="general"){
                    ctx.fillText("帅", x - 10, y);
                    ctx.strokeText("帅", x - 10, y);
                    ctx.font = "10px sans-serif";
                    ctx.fillText("shuài", x - 10, y + 10);
                    ctx.strokeText("shuài", x - 10, y + 10);
                } else if (name == "guard"){
                    ctx.fillText("仕", x - 10, y + 2);
                    ctx.strokeText("仕", x - 10, y + 2);
                    ctx.font = "10px sans-serif";
                    ctx.fillText("shì", x - 5, y + 12);
                    ctx.strokeText("shì", x - 5, y + 12);
                } else if (name == "elephant"){
                    ctx.fillText("相", x - 10, y);
                    ctx.strokeText("相", x - 10, y);
                    ctx.font = "10px sans-serif";
                    ctx.fillText("xiàng", x - 10, y + 10);
                    ctx.strokeText("xiàng", x - 10, y + 10);
                } else if (name == "horse"){
                    ctx.fillText("马", x - 10, y + 1);
                    ctx.strokeText("马", x - 10, y + 1);
                    ctx.font = "10px sans-serif";
                    ctx.fillText("mǎ", x - 6, y + 12);
                    ctx.strokeText("mǎ", x - 6, y + 12);
                } else if (name == "rook"){
                    ctx.fillText("车", x - 10, y + 1);
                    ctx.strokeText("车", x - 10, y + 1);
                    ctx.font = "10px sans-serif";
                    ctx.fillText("jū", x - 4, y + 12);
                    ctx.strokeText("jū", x - 4, y + 12);
                } else if (name=="cannon"){
                    ctx.fillText("炮", x - 10, y + 1);
                    ctx.strokeText("炮", x - 10, y + 1);
                    ctx.font = "10px sans-serif";
                    ctx.fillText("pào", x - 7, y + 12);
                    ctx.strokeText("pào", x - 7, y + 12);
                } else if (name=="soldier"){
                    ctx.fillText("兵", x - 10, y + 1);
                    ctx.strokeText("兵", x - 10, y + 1);
                    ctx.font = "10px sans-serif";
                    ctx.fillText("bīng", x - 8, y + 12);
                    ctx.strokeText("bīng", x - 8, y + 12);
                }
            }
        }*/
        
        //Piece blackGeneral = new Piece("BlackGeneral", 300 + 4 * 50, 100);
       
        //draw static pieces and game board lines
        var xStart = 300;
        var yStart = 100;
        drawGameBoard(ctx,xStart, yStart);
        /*
        drawPiece(ctx,@Model.blackGeneral.Name, @Model.blackGeneral.Color, @Model.blackGeneral.X,@Model.blackGeneral.Y);
        drawPiece(ctx,@Model.blackGuard1.Name, @Model.blackGuard1.Color, @Model.blackGuard1.X,@Model.blackGuard1.Y);
        drawPiece(ctx,@Model.blackGuard2.Name, @Model.blackGuard2.Color, @Model.blackGuard2.X, @Model.blackGuard2.Y);
        drawPiece(ctx,@Model.blackElephant1.Name, @Model.blackElephant1.Color,@Model.blackElephant1.X,@Model.blackElephant1.Y);
        drawPiece(ctx,@Model.blackElephant2.Name, @Model.blackElephant2.Color,@Model.blackElephant2.X,@Model.blackElephant2.Y);
        drawPiece(ctx,@Model.blackHorse1.Name,@Model.blackHorse1.Color,@Model.blackHorse1.X,@Model.blackHorse1.Y);
        drawPiece(ctx,@Model.blackHorse2.Name,@Model.blackHorse2.Color,@Model.blackHorse2.X,@Model.blackHorse2.Y);
        drawPiece(ctx,@Model.blackRook1.Name, @Model.blackRook1.Color,@Model.blackRook1.X,@Model.blackRook1.Y);
        drawPiece(ctx,@Model.blackRook2.Name,@Model.blackRook2.Color,@Model.blackRook2.X,@Model.blackRook2.Y);
        drawPiece(ctx,@Model.blackCannon1.Name,@Model.blackCannon1.Color,@Model.blackCannon1.X,@Model.blackCannon1.Y);
        drawPiece(ctx,@Model.blackCannon2.Name,@Model.blackCannon2.Color,@Model.blackCannon2.X,@Model.blackCannon2.Y);
        drawPiece(ctx,@Model.blackSoldier1.Name,@Model.blackSoldier1.Color,@Model.blackSoldier1.X,@Model.blackSoldier1.Y);
        drawPiece(ctx,@Model.blackSoldier2.Name,@Model.blackSoldier2.Color,@Model.blackSoldier2.X,@Model.blackSoldier2.Y);
        drawPiece(ctx,@Model.blackSoldier3.Name,@Model.blackSoldier3.Color,@Model.blackSoldier3.X,@Model.blackSoldier3.Y);
        drawPiece(ctx,@Model.blackSoldier4.Name, @Model.blackSoldier4.Color,@Model.blackSoldier4.X,@Model.blackSoldier4.Y);
        drawPiece(ctx,@Model.blackSoldier5.Name,@Model.blackSoldier5.Color,@Model.blackSoldier5.X,@Model.blackSoldier5.Y);

        drawPiece(ctx,@Model.redGeneral.Name, @Model.redGeneral.Color, @Model.redGeneral.X,@Model.redGeneral.Y);
        drawPiece(ctx,@Model.redGuard1.Name, @Model.redGuard1.Color, @Model.redGuard1.X,@Model.redGuard1.Y);
        drawPiece(ctx,@Model.redGuard2.Name, @Model.redGuard2.Color, @Model.redGuard2.X, @Model.redGuard2.Y);
        drawPiece(ctx,@Model.redElephant1.Name, @Model.redElephant1.Color,@Model.redElephant1.X,@Model.redElephant1.Y);
        drawPiece(ctx,@Model.redElephant2.Name, @Model.redElephant2.Color,@Model.redElephant2.X,@Model.redElephant2.Y);
        drawPiece(ctx,@Model.redHorse1.Name,@Model.redHorse1.Color,@Model.redHorse1.X,@Model.redHorse1.Y);
        drawPiece(ctx,@Model.redHorse2.Name,@Model.redHorse2.Color,@Model.redHorse2.X,@Model.redHorse2.Y);
        drawPiece(ctx,@Model.redRook1.Name, @Model.redRook1.Color,@Model.redRook1.X,@Model.redRook1.Y);
        drawPiece(ctx,@Model.redRook2.Name,@Model.redRook2.Color,@Model.redRook2.X,@Model.redRook2.Y);
        drawPiece(ctx,@Model.redCannon1.Name,@Model.redCannon1.Color,@Model.redCannon1.X,@Model.redCannon1.Y);
        drawPiece(ctx,@Model.redCannon2.Name,@Model.redCannon2.Color,@Model.redCannon2.X,@Model.redCannon2.Y);
        drawPiece(ctx,@Model.redSoldier1.Name,@Model.redSoldier1.Color,@Model.redSoldier1.X,@Model.redSoldier1.Y);
        drawPiece(ctx,@Model.redSoldier2.Name,@Model.redSoldier2.Color,@Model.redSoldier2.X,@Model.redSoldier2.Y);
        drawPiece(ctx,@Model.redSoldier3.Name,@Model.redSoldier3.Color,@Model.redSoldier3.X,@Model.redSoldier3.Y);
        drawPiece(ctx,@Model.redSoldier4.Name, @Model.redSoldier4.Color,@Model.redSoldier4.X,@Model.redSoldier4.Y);
        drawPiece(ctx,@Model.redSoldier5.Name,@Model.redSoldier5.Color,@Model.redSoldier5.X,@Model.redSoldier5.Y);
        */
        drawBlackGeneral(ctx,300 + 4 * 50, 100);
        drawRedGeneral(ctx,300 + 4 * 50, 100 + 9 * 45);
        
        drawBlackGuard(ctx,300 + 3 * 50, 100);
        
        drawBlackGuard(ctx,300 + 5 * 50, 100);
        
        drawRedGuard(ctx,300 + 3 * 50, 100 + 9 * 45);
        
        drawRedGuard(ctx,300 + 5 * 50, 100 + 9 * 45);
        
        drawBlackElephant(ctx,300 + 2 * 50, 100);
        
        drawBlackElephant(ctx,300 + 6 * 50, 100);
        
        drawRedElephant(ctx,300 + 2 * 50, 100 + 9 * 45);
        
        drawRedElephant(ctx,300 + 6 * 50, 100 + 9 * 45);
        
        drawBlackHorse(ctx,300 + 1 * 50, 100);
        
        drawBlackHorse(ctx,300 + 7 * 50, 100);
        
        drawRedHorse(ctx,300 + 1 * 50, 100 + 9 * 45);
        
        drawRedHorse(ctx,300 + 7 * 50, 100 + 9 * 45);
        
        drawBlackRook(ctx,300, 100);
        
        drawBlackRook(ctx,300 + 8 * 50, 100);
        
        drawRedRook(ctx,300, 100 + 9 * 45);
        
        drawRedRook(ctx,300 + 8 * 50, 100 + 9 * 45);
        
        drawBlackCannon(ctx,300 + 1 * 50, 100 + 2 * 45);
        
        drawBlackCannon(ctx,300 + 7 * 50, 100 + 2 * 45);
        
        drawRedCannon(ctx,300 + 1 * 50, 100 + 7 * 45);
        
        drawRedCannon(ctx,300 + 7 * 50, 100 + 7 * 45);
        
        
        for (let i = 0; i <= 8; i = i + 2) {
            drawBlackSoldier(ctx,300 + i * 50, 100 + 3 * 45);
            
            drawRedSoldier(ctx,300 + i * 50, 100 + 6 * 45);
        }

        //var xCell = document.getElementById("eX");
        /*
        var counter=0;
        //do something on click
        canvas.addEventListener("click", function (evt) {
            var mousePos = getMousePos(canvas, evt);
            alert(mousePos.x + ',' + mousePos.y);
            //test for mouse click on a piece
            a = mousePos.x - (Model.X);
            a2 = a * a;
            b = mousePos.y - Model.Y;
            b2 = b * b;
            if (Math.sqrt(a2 + b2) <= 20 && counter % 2 == 0) {
                    counter++;
                    canvas.addEventListener("mousemove", movePiece, false);
            } else if (Math.sqrt(a2 + b2) <= 20 && counter % 2 == 1) {
                    counter++;
                    canvas.removeEventListener("mousemove", movePiece, false);
            }
            //((mousePos.x - (300 + 4 * 50)) ^ 2 + (mousePos.y - 100) ^ 2) <= 10 ^ 2
        }, false);
        
        function movePiece(evt){
            var mousePos = getMousePos(canvas, evt);
                    ctx.clearRect(0, 0, canvas.width, canvas.height);
                    drawGameBoard(ctx, 300, 100);
                    drawRedGeneral(ctx, 300 + 4 * 50, 100 + 9 * 45);
                    drawBlackGuard(ctx, 300 + 3 * 50, 100);
                    drawBlackGuard(ctx, 300 + 5 * 50, 100);
                    drawRedGuard(ctx, 300 + 3 * 50, 100 + 9 * 45);
                    drawBlackElephant(ctx, 300 + 2 * 50, 100);
                    drawBlackElephant(ctx, 300 + 6 * 50, 100);
                    drawRedGuard(ctx, 300 + 5 * 50, 100 + 9 * 45);
                    drawRedElephant(ctx, 300 + 2 * 50, 100 + 9 * 45);
                    drawRedElephant(ctx, 300 + 6 * 50, 100 + 9 * 45);
                    drawBlackHorse(ctx, 300 + 1 * 50, 100);
                    drawBlackHorse(ctx, 300 + 7 * 50, 100);
                    drawRedHorse(ctx, 300 + 1 * 50, 100 + 9 * 45);
                    drawRedHorse(ctx, 300 + 7 * 50, 100 + 9 * 45);
                    drawBlackRook(ctx, 300, 100);
                    drawBlackRook(ctx, 300 + 8 * 50, 100);
                    drawRedRook(ctx, 300, 100 + 9 * 45);
                    drawRedRook(ctx, 300 + 8 * 50, 100 + 9 * 45);
                    drawBlackCannon(ctx, 300 + 1 * 50, 100 + 2 * 45);
                    drawBlackCannon(ctx, 300 + 7 * 50, 100 + 2 * 45);
                    drawRedCannon(ctx, 300 + 1 * 50, 100 + 7 * 45);
                    drawRedCannon(ctx, 300 + 7 * 50, 100 + 7 * 45);
                        for (let i = 0; i <= 8; i = i + 2) {
                            drawBlackSoldier(ctx, 300 + i * 50, 100 + 3 * 45);
                            drawRedSoldier(ctx, 300 + i * 50, 100 + 6 * 45);
                        }
                    drawBlackGeneral(ctx, mousePos.x, mousePos.y);
                    ctx.fillStyle="black";
                    ctx.strokeText(counter,100,100);
        }
        
        function erase(canvas, evt){
            var rect = canvas.getBoundingClientRect();
            var x= evt.clientX - rect.left;
            var y= evt.clientY - rect.top;
            cutCircle(ctx,x,y,20);
        }
        
        //Get Mouse Position
        function getMousePos(canvas, evt) {
            var rect = canvas.getBoundingClientRect();
            return {
                x: evt.clientX - rect.left,
                y: evt.clientY - rect.top
            };
        }
       */
    </script>
    <p id="block1">I love you Sophia</p>
</body>

